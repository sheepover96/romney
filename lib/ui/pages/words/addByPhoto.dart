import 'dart:async';
import 'package:flutter/material.dart';

import 'package:image_picker/image_picker.dart';

class AddByPhoto extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final picker = ImagePicker();
  }
}
